<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen p-5 bg-zinc-900">
        <div class="form text-white">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 w-full rounded-lg mb-2"type="text" placeholder="title" name="title"/>
            <textarea class="block px-5 py-2 bg-zinc-800 resize-none w-full rounded-lg"placeholder="task details" name="details"></textarea>   
            <input class="mt-2 bg-emerald-400 p-4 rounded-md"type="submit" value="Create Task">     
            </form>        
        </div>
        <div class="task p-10 flex gap-4 flex-wrap">
            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task w-72 px-3 py-4 rounded-md bg-zinc-800 ">
                        <h1 class="text-white text-2xl tracking-tighter "><%= val %></h1>
                        <a class="text-blue-500 inline-block mt-3"href="/file/<%= val %>">Read more</a> 
                        <a href="/edit/<%= val %>">edit name</a> 
                    
                    </div>
                <% }) %>
            <% }else{ %>
                <h3 class="text-zinc-500">No tasks created</h3>
            <% } %>
            
            
            
            

        </div>
    </div>



    <script src="/javascripts/script.js"></script>
</body>
</html>